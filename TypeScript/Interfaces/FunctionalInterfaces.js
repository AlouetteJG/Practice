var add;
var sub;
add = function (x, y) {
    return x + y;
};
sub = function (a, b) {
    return a - b;
};
sub = function (a, b) {
    return "The numbers are " + a + b;
};
console.log(sub(5, 10));
console.log(add(5, 10));
